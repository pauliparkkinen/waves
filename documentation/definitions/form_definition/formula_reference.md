# Definition

A formula reference is one referenced entity in a "formula". The formula reference can refer to another "formula" or "activity".

# Parameters
- formula_reference_id
- formula_id -- Id of the formula
- symbol -- Symbol used in the formula visualization
- type -- Type of the reference "formula" | "activity"
- referenced_formula_id -- If type is "formula"