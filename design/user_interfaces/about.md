# Description

There are three separate user interfaces in  the application: admin, hcp and patient. The features of each interface is defined in similarly named folder. In addition, there are features that are shared between the interfaces. These features are defined in the 'common' folder.

# Requirements

The user interface must follow WCAG 2.2 (https://www.w3.org/TR/WCAG22/) standards.
